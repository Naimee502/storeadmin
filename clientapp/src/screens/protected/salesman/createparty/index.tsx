// Salesman create-party — reuses the staff create-party form. When opened with
// route params (routeId/day) it also assigns the new party to the salesman's route.
export { default } from '../../staff/createparty';
