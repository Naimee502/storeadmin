export const API_CONFIG = {
  /**
   * GraphQL endpoint of your server.
   * Android emulator: use 10.0.2.2 to reach localhost on the host machine.
   * Physical device: use your machine's local IP, e.g. 192.168.1.5:4000
   */
  GRAPHQL_URL: 'http://10.0.2.2:4000/graphql',

  TIMEOUT: 30000,
};
